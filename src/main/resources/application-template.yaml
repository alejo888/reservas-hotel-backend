spring:
  application:
    name: reservas-backend
  jpa:
    database: postgresql
    show-sql: false
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/reservas_hotel
    username: YOUR_DB_USER
    password: YOUR_DB_PASSWORD
